# Temporary Railway worker branch ONLY. Never merge this file into main.
# No pre-deploy migration, no backend server, no database.
[build]
installCommand = "npm ci --omit=dev"
watchPatterns = ["scripts/cj-three-readonly-railway-once.js", "services/suppliers/connectors/cj-connector.js", "railway.toml", "package.json", "package-lock.json"]

[deploy]
startCommand = "node scripts/cj-three-readonly-railway-once.js"
restartPolicyType = "NEVER"
