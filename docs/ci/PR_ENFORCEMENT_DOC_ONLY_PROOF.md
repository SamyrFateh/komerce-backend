# PR enforcement — preuve doc-only

Fichier temporaire destiné uniquement à prouver qu'une PR documentaire ne déclenche pas les gates backend du workflow `PR enforcement`.

Attendu :
- `Detect PR scope` : success ;
- `Backend gates` : skipped ;
- `Required verdict` : success.

Cette branche/PR de preuve ne doit pas être mergée.
