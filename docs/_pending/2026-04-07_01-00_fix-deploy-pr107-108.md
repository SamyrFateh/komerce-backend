# Delta — PR #107 + #108 — Fix Déploiement + Crash

> Date: 2026-04-07
> PRs: #107 (railway.toml), #108 (fix sms.js)

## Résumé

- PR #107 : Ajout `railway.toml` avec watch patterns pour filtrer les déploiements docs-only sur Railway
- PR #108 : Fix SyntaxError dans `utils/sms.js` ligne 174 — backticks imbriquées dans template literals SQL

## Fichiers impactés

| Fichier | Action | PR |
|---------|--------|-----|
| `railway.toml` | Ajouté | #107 |
| `utils/sms.js` | Modifié (SHA af6ae916 → 995ee53f) | #108 |

## Impact

- **Déploiement** : ~70% de builds Railway en moins (commits docs-only ignorés)
- **Runtime** : Fix crash au démarrage (SyntaxError `missing ) after argument list`)
- **Sécurité** : Les requêtes SQL dans sms.js utilisent maintenant des paramètres `$1`, `$2` (plus sûr)

## Tables impactées

Aucune

## Endpoints impactés

Aucun directement (fix infrastructure)
