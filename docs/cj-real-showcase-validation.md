# CJ real showcase validation

Validation is performed by `.github/workflows/cj-real-showcase-contract.yml` on pull requests touching the CJ real-showcase bootstrap. The workflow checks JavaScript syntax and the deterministic 21 × 3 contract test before merge.
