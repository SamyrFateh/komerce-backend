name: Lot 8 post-merge reconcile once

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Workflow parked while the resurrection gate is corrected.'
