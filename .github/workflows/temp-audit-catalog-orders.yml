name: Temporary audit catalog orders seam

on:
  push:
    branches:
      - governance/catalog-orders-boundary

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Print precise symbol ownership evidence
        run: node scripts/temp-audit-catalog-orders-symbols.js
