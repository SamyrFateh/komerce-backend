# Runtime

Baselines SHA-256 temporaires créées lors de `agent-start.ps1`.


Les reçus de packages émis sont stockés dans `issued-packages/`. Ils servent à
reconnaître les livraisons au retour sans modifier les fichiers suivis par Git.
