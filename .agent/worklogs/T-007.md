# WORKLOG — T-007

- Tâche : M6 — Série produit dans la meta hero
- Lane : LANE-META
- Branche : agent/lane-mobile-renderer

## 2026-07-18 — Option D implémentée

- Extension nullable `products.series` ajoutée via migration 112.
- Contrat Product Detail v1 étendu avec `product.series` (`string|null`).
- Service backend et renderers mobile/desktop branchés avec fallback silencieux.
- Tests boutique mobile et desktop verts selon les preuves de l’agent.
- Revue humaine : `REQUEST_CHANGES`, car le test backend strict du contrat attendait encore l’ancien objet sans `series`.

## 2026-07-18 — Correction backend poussée

- Commit : `ffc0b21160f88fc95cc33283f9cfe0df40004811`.
- `tests/unit/catalog-product-detail.test.js` mis à jour :
  - `series: null` dans la fixture par défaut et l’assertion stricte ;
  - mapping explicite d’une série non nulle ;
  - compatibilité `null` ;
  - vérification de la réponse HTTP.
- Syntaxe JavaScript vérifiée avec `node --check` : PASS.
- Preuve : `.agent/evidence/T-007/backend-contract-review.txt`.
- Les tests Jest n’ont pas pu être exécutés depuis l’environnement de revue, qui ne contient pas les dépendances du dépôt et n’expose aucun check CI pour ce commit.
- Statut attendu : `REVIEW` jusqu’à exécution de :
  - `npm test -- tests/unit/catalog-product-detail.test.js`
  - `npm test`

## 2026-07-19 — Clôture (test golden + doctrine)

- Décision reviewer : `series` reste dans le contrat backend ; c'est le test golden GPM-1 qui était obsolète.
- `tests/unit/golden-product-gpm1.test.js` : ajout de `series: null` à l'objet `detail.product` attendu.
- `docs/doctrine/DOCTRINE_PRODUCT_DETAIL_CONTRACT.md` : ajout de `series` à l'exemple JSON du bloc `product`, précision de nullabilité/source (`products.series`), version bumpée à 1.2 (2026-07-19).
- Preuves :
  - `.agent/evidence/T-007/unit-catalog-and-golden.txt` — `npm test -- tests/unit/catalog-product-detail.test.js tests/unit/golden-product-gpm1.test.js` → 29/29 PASS.
  - `.agent/evidence/T-007/unit-all.txt` — `npm test -- tests/unit` → 6086 passed, 0 failed (345/346 suites, 1 skip).
  - `.agent/evidence/T-007/full-suite.txt` — `npm test` → 6200 passed, 6 suites d'intégration en échec (`txg01`, `txg02`, `txg03`, `txg04`, `txg05`, `r6-crash-window`) avec `ECONNREFUSED 127.0.0.1:5432`.
- **Dérogation d'environnement documentée** : le sandbox de revue ne dispose d'aucune instance PostgreSQL. Les 6 échecs restants sont uniquement des refus de connexion réseau vers `127.0.0.1:5432`, confirmés identiques avant et après ce lot ; aucune assertion applicative n'échoue dans ces suites. Aucune assertion ou suite unitaire ne reste rouge.
- Statut : `REVIEW` → `DONE`, `review_decision: APPROVE`.

## 2026-07-19 — Commit de cohérence documentaire

- `.agent/state/T-007.json` : `last_commit` → `8d077ef32eb1588db84758e0a277b5dbebfa7e98` ; `allowed_files` complété avec `tests/unit/golden-product-gpm1.test.js` et `docs/doctrine/DOCTRINE_PRODUCT_DETAIL_CONTRACT.md`. Statut `DONE` et `review_decision: APPROVE` inchangés.
- `.agent/tasks/T-007.md` : documentation de l'extension de périmètre Option D (backend + golden test + doctrine), périmètre autorisé complété, 3 critères d'acceptation cochés, gates backend réellement exécutés ajoutés, code fence `powershell` → `bash`, dérogation PostgreSQL documentée.
- **Conflit CSS Guardian `#k-modal-payment` (hook pre-commit)** : vérifié séparément par bissection `git worktree` + exécution isolée de `node scripts/css-guard.js --strict` sur l'historique. Absent jusqu'au commit `dd102724` (T-016, D-P1), apparaît au commit `e7872666` (T-016, D4 pt.2-7 — réorganisation livraison/paiement). Confirmé : préexiste intégralement avant tout commit T-007 (`75903513` est antérieur à `e7872666`). Aucune correction CSS effectuée dans ce lot. Enregistré comme dette distincte, hors périmètre T-007, dans `.agent/evidence/T-007/css-guardian-preexisting-debt.txt` — à traiter sous T-016 / lane livraison-paiement.
