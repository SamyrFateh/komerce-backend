# WORKLOG — T-020

- Tâche : D8 — Grille desktop 60/1fr/310 puis 64/1fr/340
- Lane : LANE-DESKTOP-LAYOUT
- Branche : agent/lane-mobile-renderer

## 2026-07-19 — REVIEW

- Le travail initial avait été poussé sur la branche accidentelle
  `agent/lane-desktop-layout`. La branche durable avait divergé indépendamment;
  aucun merge global ni cherry-pick brut n'a été utilisé.
- Le code T-020 a été reporté hunk par hunk dans
  `modal-product-lot4-hybrid.css`, `modal-shell.css` et `index.html`, puis poussé
  sur la branche durable au commit
  `abedc195d0568f266bf9750313b678b88eeab49f`.
- L'arbitrage ownership a été rapatrié sur la branche durable :
  `modal-product-lot4-hybrid.css` possède `grid-template-columns`, `gap` et
  `padding`; `modal-shell.css` possède `grid-column` et `grid-row` des enfants.
- Composition obtenue à partir de 1024px : rail miniatures | média | détails,
  soit `60px minmax(420px, 1fr) 310px`, puis
  `64px minmax(480px, 1fr) 340px` à partir de 1440px.
- Le comportement 900–1023px reste à deux colonnes. L'override concurrent
  `grid-template-columns` du bloc 900–1120px a été retiré.
- Les preuves ont été extraites et poussées séparément au commit
  `0df83190d2ef5663ab21e18a6036222c81fab98a` : captures 1024, 1440 et 1600,
  mesures calculées et résultat render-static.
- Mesures enregistrées : détails à 310px à 1024, 340px à 1440/1600; wrapper
  média sur les pistes 1–2; détails/actions sur la piste 3; quatre miniatures
  visibles; aucun débordement horizontal.
- Les gates ont été réexécutés sur `agent/lane-mobile-renderer`, puis leurs
  artefacts ont été poussés au commit
  `d33d5a0b6cf167cc59370b1e38a53b818967a35f`.
- `deploy:css`, `check:cache`, `check:breakpoints`, `check:css-guard`,
  `check:css-specificity-guard`, `audit:ownership`, `gate:feature-audit` et
  l'audit npm sont verts.
- Un sur-bump parasite de `base.css` et `desktop.css`, dû à une incohérence de
  méthode de hash entre scripts, a été corrigé avant le checkpoint des gates.
- Le conflit css-guard restant est préexistant, hors périmètre T-020 et lié à
  T-018; son absence de régression a été confirmée avant gel de baseline.
- Un bug distinct de `scripts/agent-checkpoint.mjs` (`trim()` global tronquant
  le premier chemin dirty) a été corrigé dans un checkpoint d'outillage séparé.
- Statut : `READY` → `REVIEW`.
- Prochaine action : revue humaine du diff, de l'arbitrage ownership et des
  captures 1024/1440/1600, puis décision `DONE` ou retour agent.
