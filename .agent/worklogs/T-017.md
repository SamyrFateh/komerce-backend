# Worklog T-017 — D5 — Validation série sur desktop

## Constat

L'implémentation desktop de `product.series` existe déjà (T-007, `b-modal-desktop-product.js`
lignes 82-88) : lecture de `detail.product.series`, `textContent` conditionnel, `hidden`
booléen — fallback silencieux, aucune ligne vide. Le pattern est identique à celui de
`b-modal-mobile-product.js` (lignes 206-211).

Aucun code desktop manquant identifié : rien à ajouter au sens strict de l'action attendue
point 3.

## Vérifications effectuées

- Recherche exhaustive de `series` dans `public/boutique/js/` : seules les deux implémentations
  (mobile, desktop) existent, aucune fonction partagée dédiée. La logique (3 lignes) est
  dupliquée à l'identique entre mobile et desktop depuis T-007 — préexistant, hors périmètre
  autorisé de T-017 (qui n'inclut pas `b-modal-mobile-product.js`) pour une factorisation.
- Tests unitaires dédiés déjà présents et verts : `b-modal-desktop-product.test.js`, suite
  « série produit meta hero (M6, spec §9.1, contrat v1 product.series) » — 2/2 PASS
  (présence affichée, absence masquée sans ligne vide).
- Gates complètes rejouées : `check:imports`, `audit:arch`, `audit:ownership`, `audit:gate`,
  `test:unit` (92 suites / 1751 tests) — toutes PASS. Voir `.agent/evidence/T-017/tests.txt`.

## Blocage

Preuves visuelles attendues (`desktop-series-1024.png`, `desktop-series-1440.png`)
non générées : aucun binaire Chromium/Playwright disponible dans ce sandbox, et le
réseau est restreint aux registres npm/pip/GitHub (pas d'accès au CDN de téléchargement
des navigateurs Playwright). Même cause de blocage que T-023.

## Déblocage — 2026-07-19

Un binaire Chromium pré-installé a été trouvé dans le sandbox
(`~/.cache/puppeteer/chrome/linux-131.0.6778.204/chrome-linux64/chrome`, Google
Chrome for Testing 131.0.6778.204), non détecté par `npx playwright --version`
car hors du cache attendu par Playwright, mais utilisable directement via
`executablePath`.

Captures générées avec la méthode harnais statique + bundles CSS réels
(identique à T-002/T-003/T-004, cf. `.agent/evidence/T-017/CAPTURE-METHOD.md`) :
- `desktop-series-1024.png`, `desktop-series-1440.png` — série affichée
  correctement aux deux largeurs.
- `desktop-series-1024-fallback.png`, `desktop-series-1440-fallback.png` —
  fallback silencieux vérifié visuellement (aucune ligne vide).

## Statut

`REVIEW` — code déjà conforme (T-007), gates 5/5 vertes, tests unitaires
2/2 sur la suite série desktop, captures 1024/1440 générées et vérifiées
visuellement (série affichée + fallback silencieux sans ligne vide).
