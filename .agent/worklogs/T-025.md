# Worklog T-025 — D13 — Ombre modale gris chaud

## 2026-07-19T18:12:00.000Z — DONE (récupération depuis agent/lane-finish)

- Contexte : le resolver (`node scripts/agent-bootstrap.mjs`) a correctement
  sélectionné T-025 avec `EFFECTIVE_STATUS=RECOVER_TO_DURABLE` :
  travail existant sur `agent/lane-finish` (commit `99dfe84f`), non intégré
  à la branche durable `agent/lane-mobile-renderer`.
- Action : récupération manuelle du seul périmètre T-025, sans
  réimplémentation et sans import du reste de `agent/lane-finish` (T-024
  était déjà intégré différemment sur `lane-mobile-renderer`, T-023/T-027
  préservés tels quels) :
  - `tokens.css` : ajout de `--shadow-modal-neutral-10: 0 24px 60px rgba(31,48,36,.10);`
  - `modal-shell.css` ligne ~810 (règle gagnante `#k-modal.k-modal`) :
    `box-shadow: none` → `box-shadow: var(--shadow-modal-neutral-10)`.
  - `modal-shell.css` ligne ~969 (règle morte `.k-modal`) :
    `box-shadow: 0 30px 80px var(--border-ocean-18)` → même token neutre.
  - Preuves `.agent/evidence/T-025/token-audit.txt` et `agent-gates.json`
    rapatriées depuis `agent/lane-finish`, complétées d'une note
    d'intégration et des résultats de gates ré-exécutés ici.
- Gates : 6/6 PASS, ré-exécutés intégralement sur `agent/lane-mobile-renderer`
  (structure du fichier ayant divergé depuis T-020/T-021/T-024 déjà
  intégrés sur cette branche).
- Checkpoints distants :
  - `d6bf21ef` — code source (`tokens.css`, `modal-shell.css`)
  - `8db5742c` — bundles régénérés (`deploy:css`)
  - `60e1a54a` — carte de propriété régénérée (`audit:ownership`)
  - `b88c2a43` — preuves T-025
- Commit : b88c2a4377d42e8d2e661f61fa1916b82c55ada6
- Prochaine action : laisser le resolver choisir naturellement la prochaine
  tâche READY de LANE-FINISH (T-028 attendu).
