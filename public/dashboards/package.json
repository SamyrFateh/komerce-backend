{
  "name": "dashboards",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "audit:registry": "node scripts/feature-registry-check.js --strict",
    "check:all": "npm run quality:gate && npm run arch:check && npm run feature:guard && npm run audit:registry && npm run test:unit",
    "precommit": "npm run check:all",
    "audit:gate": "npm audit --production --audit-level=high || echo \"Audit: vulnérabilités détectées\"",
    "quality:gate": "node scripts/code-quality-gate.js --strict",
    "arch:check": "node scripts/arch-check.js --strict",
    "feature:guard": "node scripts/feature-guard.js --strict",
    "test:unit": "npx jest --no-coverage --forceExit"
  },
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^8.0.1",
    "@babel/preset-env": "^8.0.2",
    "babel-jest": "^30.4.1",
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1"
  }
}