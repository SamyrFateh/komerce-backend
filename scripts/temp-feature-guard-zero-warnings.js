'use strict';
const fs = require('fs');

function read(p) { return fs.readFileSync(p, 'utf8'); }
function write(p, s) { fs.writeFileSync(p, s); }
function replaceOnce(p, from, to) {
  const src = read(p);
  if (!src.includes(from)) throw new Error(`marker not found in ${p}: ${from.slice(0, 80)}`);
  write(p, src.replace(from, to));
}
function insertAfter(p, marker, line) {
  const src = read(p);
  if (src.includes(line.trim())) return;
  if (!src.includes(marker)) throw new Error(`insert marker not found in ${p}: ${marker}`);
  write(p, src.replace(marker, `${marker}\n${line}`));
}

// 1) Feature Guard: a gap between migration numbers inside one feature is not a signal.
// Komerce allocates migration slots repo-wide while features evolve at different cadence.
{
  const p = 'scripts/feature-guard.js';
  let src = read(p);
  const start = src.indexOf('  // ── 4. Migrations séquentielles (staging+)');
  const end = src.indexOf('  // ── 5. Couverture tests structurelle (staging+)');
  if (start < 0 || end < 0 || end <= start) throw new Error('feature-guard migration block markers missing');
  const replacement = `  // ── 4. Migrations — convention de créneaux repo-wide ───────────────────\n  // Komerce ne numérote PAS les migrations de façon dense par feature. Une\n  // feature peut légitimement passer de 086 à 131 parce que les créneaux\n  // intermédiaires appartiennent à d'autres features. La collision exacte de\n  // créneau est déjà vérifiée ci-dessus ; l'ordre/hash/applicabilité sont\n  // couverts par les gates migration/schema dédiées. Un \"gap > 20\" n'est\n  // donc ni une dette ni un warning exploitable et ne doit pas être émis ici.\n\n`;
  src = src.slice(0, start) + replacement + src.slice(end);

  const old = `    const tests = new Set(slice.files.tests || []);\n    for (const rel of [...(slice.files.services || []), ...(slice.files.routes || [])]) {`;
  const neu = `    const tests = new Set(slice.files.tests || []);\n    // Un composition root peut être dupliqué dans files.routes uniquement pour\n    // rattacher des endpoints inline au graphe, tout en restant déclaré comme\n    // config. Dans ce cas ce n'est pas un routeur autonome à couvrir par un\n    // test homonyme (ex. server.js).\n    const routeAliasesForGraph = new Set(slice.files.config || []);\n    for (const rel of [...(slice.files.services || []), ...(slice.files.routes || [])]) {\n      if (routeAliasesForGraph.has(rel)) continue;`;
  if (!src.includes(old)) throw new Error('feature-guard coverage marker missing');
  src = src.replace(old, neu);
  write(p, src);
}

// 2) Dead gap exemptions: retain only true historical filename exceptions.
{
  const p = 'governance/migration-guard-exemptions.json';
  const obj = JSON.parse(read(p));
  obj._comment = 'Exemptions documentées pour feature-guard.js — uniquement fichiers historiques sans préfixe numérique.';
  obj._règle = 'Ce fichier ne grandit que pour documenter des fichiers historiques immuables. Les gaps numériques intra-feature ne sont pas un signal : Komerce alloue les créneaux de migration repo-wide, pas de façon dense par feature.';
  delete obj.gaps;
  write(p, JSON.stringify(obj, null, 2) + '\n');
}

// 3) Declare the five new direct tests in canonical feature manifests.
insertAfter('features/catalog.feature.js', "      'tests/unit/json-source-pipeline.test.js',", "      'tests/unit/json-connector.test.js',");
insertAfter('features/orders.feature.js', "      'tests/unit/qr.test.js',", "      'tests/unit/qr-collection-core.test.js',");
insertAfter('features/payments.feature.js', "      'tests/unit/payment-service.test.js',", "      'tests/unit/payment-status-validator.test.js',");
insertAfter('features/platform-ops.feature.js', "      'tests/unit/monitoring.test.js',", "      'tests/unit/simulator-cleanup.test.js',");
insertAfter('features/shared-cart.feature.js', "      'tests/unit/shared-cart-creator-route.test.js',", "      'tests/unit/shared-cart-saved.test.js',");

// 4) Direct unit tests.
write('tests/unit/payment-status-validator.test.js', `'use strict';\n\n/** @test-kind unit @test-runner jest @test-requires none */\nconst { validateTransition, sourceStatusesFor, sqlGuard } = require('../../services/payment-status-validator');\n\ndescribe('payment-status-validator', () => {\n  test('refunded -> paid reste terminal', () => {\n    expect(validateTransition('refunded', 'paid')).toEqual({ allowed: false, reason: 'blocked:refunded_to_paid' });\n  });\n\n  test('failed -> paid exige un événement de paiement identifiable', () => {\n    expect(validateTransition('failed', 'paid').allowed).toBe(false);\n    expect(validateTransition('failed', 'paid', { paymentEvent: { type: 'paypal.capture', externalId: 'cap-1' } }).allowed).toBe(true);\n  });\n\n  test('partially_paid ne peut jamais être écrit', () => {\n    expect(validateTransition('pending', 'partially_paid')).toEqual({ allowed: false, reason: 'partially_paid_never_written' });\n  });\n\n  test('les sources SQL dérivent de la même matrice', () => {\n    expect(sourceStatusesFor('refunded')).toEqual(['paid']);\n    expect(sqlGuard(['paid'])).toBe("payment_status = 'paid'");\n    expect(sqlGuard([])).toBe('FALSE');\n  });\n});\n`);

write('tests/unit/simulator-cleanup.test.js', `'use strict';\n\n/** @test-kind unit @test-runner jest @test-requires none */\njest.mock('../../db', () => ({ query: jest.fn() }));\nconst db = require('../../db');\nconst { cleanup } = require('../../services/simulator/cleanup');\n\ndescribe('simulator cleanup', () => {\n  beforeEach(() => jest.clearAllMocks());\n\n  test('supprime uniquement les artefacts simulateur et retourne les compteurs', async () => {\n    db.query\n      .mockResolvedValueOnce({ rowCount: 3 })\n      .mockResolvedValueOnce({ rowCount: 2 })\n      .mockResolvedValueOnce({ rowCount: 4 });\n    await expect(cleanup()).resolves.toEqual({\n      deleted: { parcel_items: 3, parcels: 2, scans: 4 },\n      errors: [],\n    });\n    expect(db.query).toHaveBeenCalledTimes(3);\n    expect(db.query.mock.calls[0][0]).toContain('DELETE FROM parcel_items');\n    expect(db.query.mock.calls[1][0]).toContain('DELETE FROM parcels');\n    expect(db.query.mock.calls[2][0]).toContain('DELETE FROM scans');\n  });\n\n  test('une erreur DB est rendue visible sans faux succès', async () => {\n    db.query.mockRejectedValueOnce(new Error('db-down'));\n    await expect(cleanup()).resolves.toEqual({ deleted: {}, errors: ['db-down'] });\n  });\n});\n`);

write('tests/unit/shared-cart-saved.test.js', `'use strict';\n\n/** @test-kind unit @test-runner jest @test-requires none */\njest.mock('../../middleware/auth', () => ({\n  authenticate: (req, _res, next) => { req.user = { id: 'user-1' }; next(); },\n}));\njest.mock('../../services/shared-cart-library', () => ({\n  removeSavedSharedCartForUser: jest.fn(),\n}));\n\nconst express = require('express');\nconst request = require('supertest');\nconst { removeSavedSharedCartForUser } = require('../../services/shared-cart-library');\n\nfunction makeApp() {\n  const app = express();\n  app.use(express.json());\n  app.use('/api/shared-carts/saved', require('../../routes/shared-cart-saved'));\n  app.use((err, _req, res, _next) => res.status(500).json({ error: err.message }));\n  return app;\n}\n\ndescribe('DELETE /api/shared-carts/saved/:sharedCartId', () => {\n  beforeEach(() => jest.clearAllMocks());\n\n  test('retire seulement le signet de l’utilisateur courant', async () => {\n    removeSavedSharedCartForUser.mockResolvedValue({ removed: true, shared_cart_id: 'sc-1' });\n    const res = await request(makeApp()).delete('/api/shared-carts/saved/sc-1');\n    expect(res.status).toBe(200);\n    expect(removeSavedSharedCartForUser).toHaveBeenCalledWith('user-1', 'sc-1');\n    expect(res.body.removed).toBe(true);\n  });\n\n  test('propage une erreur métier structurée sans la convertir en 500', async () => {\n    removeSavedSharedCartForUser.mockRejectedValue(Object.assign(new Error('Liste introuvable'), { status: 404, code: 'not_found' }));\n    const res = await request(makeApp()).delete('/api/shared-carts/saved/missing');\n    expect(res.status).toBe(404);\n    expect(res.body.code).toBe('not_found');\n  });\n\n  test('une erreur inconnue va au handler central', async () => {\n    removeSavedSharedCartForUser.mockRejectedValue(new Error('boom'));\n    const res = await request(makeApp()).delete('/api/shared-carts/saved/sc-1');\n    expect(res.status).toBe(500);\n  });\n});\n`);

write('tests/unit/qr-collection-core.test.js', `'use strict';\n\n/** @test-kind unit @test-runner jest @test-requires none */\njest.mock('../../utils/logger', () => ({ child: () => ({ info: jest.fn(), warn: jest.fn(), error: jest.fn() }) }));\njest.mock('../../services/order-status-machine', () => ({ transitionOrderStatus: jest.fn() }));\njest.mock('../../utils/parcelSync', () => ({ safeSyncScanToParcels: jest.fn() }));\n\nconst { transitionOrderStatus } = require('../../services/order-status-machine');\nconst { safeSyncScanToParcels } = require('../../utils/parcelSync');\nconst { resolveQrCollection, issueOrRotateQrToken } = require('../../services/qr-collection-core');\n\ndescribe('qr-collection-core', () => {\n  beforeEach(() => jest.clearAllMocks());\n\n  test('collection: commande absente -> rollback + 404', async () => {\n    const client = { query: jest.fn().mockResolvedValueOnce({ rows: [] }).mockResolvedValueOnce({}) };\n    const out = await resolveQrCollection({ client, token: 'tok', user: { id: 'u1', role: 'customer' } });\n    expect(out.ok).toBe(false);\n    expect(out.response.status).toBe(404);\n    expect(client.query).toHaveBeenLastCalledWith('ROLLBACK');\n  });\n\n  test('collection valide passe par la machine de statut et invalide le QR', async () => {\n    const order = { id: 'o1', reference: 'K-1', status: 'available', qr_token: 'tok', qr_expires_at: new Date(Date.now() + 60000), relais_name: 'R1' };\n    const client = { query: jest.fn()\n      .mockResolvedValueOnce({ rows: [order] })\n      .mockResolvedValueOnce({ rowCount: 1 })\n      .mockResolvedValueOnce({ rows: [{ id: 'scan-1' }] }) };\n    transitionOrderStatus.mockResolvedValue({ success: true });\n    safeSyncScanToParcels.mockResolvedValue(undefined);\n    const out = await resolveQrCollection({ client, token: 'tok', user: { id: 'u1', role: 'customer' } });\n    expect(out.ok).toBe(true);\n    expect(transitionOrderStatus).toHaveBeenCalledWith(expect.objectContaining({ orderId: 'o1', newStatus: 'collected', dbClient: client }));\n    expect(client.query.mock.calls[1][0]).toContain('qr_token = NULL');\n    expect(safeSyncScanToParcels).toHaveBeenCalledWith(expect.objectContaining({ order_id: 'o1', step: 'collected' }), client);\n  });\n\n  test('émission QR: rotation atomique sur commande available', async () => {\n    const order = { id: 'o1', reference: 'K-1', status: 'available', qr_token: 'old' };\n    const client = { query: jest.fn().mockResolvedValueOnce({ rows: [order] }).mockResolvedValueOnce({ rowCount: 1 }) };\n    const out = await issueOrRotateQrToken({ client, orderId: 'o1', expirationHours: 1 });\n    expect(out.ok).toBe(true);\n    expect(out.rotated).toBe(true);\n    expect(out.token).toMatch(/^[a-f0-9]{48}$/);\n    expect(client.query.mock.calls[1][0]).toContain('UPDATE orders SET qr_token');\n  });\n});\n`);

write('tests/unit/json-connector.test.js', `'use strict';\n\n/** @test-kind unit @test-runner jest @test-requires none */\nconst connector = require('../../services/suppliers/connectors/json-connector');\n\nfunction profile(overrides = {}) {\n  return {\n    profile_id: 'UNIT_JSON', profile_version: 1, supplier_name: 'UNIT', source_type: 'json',\n    currency: { default: 'AED', resolution_policy: 'SOURCE_THEN_DEFAULT', allowed: ['AED', 'USD'] },\n    identity: { supplier_id_field: 'id' },\n    media: { gallery_source_field: 'images', thumbnail_fallback: true },\n    weight: { source_field: 'weight', source_unit: null, target_unit: 'kg', unknown_unit_policy: 'PRESERVE_RAW_AND_OMIT' },\n    policies: { unsupported_video: 'QUARANTINE_PRODUCT', lossy_mapping: 'QUARANTINE_PRODUCT', duplicate_relation: 'DEDUPLICATE_AND_AUDIT', asset_reuse: 'ALLOW_AND_AUDIT', missing_brand: 'ALLOW_NULL', missing_image: 'ALLOW_WITH_WARNING', unknown_fields: 'PRESERVE_RAW' },\n    batch: { max_products: 100, max_file_bytes: 1000000, allow_empty_products: false, max_invalid_pct: 60, max_quarantined_pct: 60, max_field_bytes: 65536, max_depth: 12 },\n    ...overrides,\n  };\n}\n\ndescribe('json-connector', () => {\n  test('préflight refuse une enveloppe JSON invalide avant toute classification', () => {\n    expect(() => connector.preflight({ source: { products: 'not-array' }, import_profile: profile() }))\n      .toThrow(expect.objectContaining({ code: 'BATCH_SOURCE_FORMAT_ERROR' }));\n  });\n\n  test('fetchProducts conserve les lignes valides et rejetées sans perte silencieuse', () => {\n    const source = { products: [\n      { id: 'sku-1', title: 'Produit', price: 19.99, stock: 2, images: ['https://example.test/a.jpg'] },\n      { title: 'Sans identifiant', price: 10, stock: 1, images: [] },\n    ] };\n    const out = connector.fetchProducts({ source, import_profile: profile(), source_bytes: Buffer.byteLength(JSON.stringify(source)) });\n    expect(out.connector_contract_version).toBe('1');\n    expect(out.statistics.total).toBe(2);\n    expect(out.ready.length + out.quarantined.length + out.rejected.length).toBe(2);\n    expect(out.rejected).toHaveLength(1);\n    expect(out.rejected[0].reason_code).toBe('MISSING_SUPPLIER_PRODUCT_ID');\n  });\n\n  test('la proposition de statut de batch reflète les seuils sans jeter les entrées', () => {\n    const source = { products: [{ title: 'A' }, { title: 'B' }] };\n    const out = connector.fetchProducts({ source, import_profile: profile({ batch: { ...profile().batch, max_invalid_pct: 10 } }) });\n    expect(out.statistics.total).toBe(2);\n    expect(out.statistics.threshold_evaluation.invalid_exceeded).toBe(true);\n    expect(out.statistics.threshold_evaluation.proposed_batch_status).toBe('BLOCKED_INVALID_THRESHOLD');\n  });\n});\n`);

console.log('Feature Guard warning cleanup staged.');
